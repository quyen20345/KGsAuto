version: '3.8'

services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: always

  neo4j:
    image: neo4j:2026.01.4     
    container_name: neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - ${Browser}/neo4j/data:/data
      - ${Browser}/neo4j/logs:/logs
      - ${Browser}/neo4j/plugins:/plugins
      - ${Browser}/neo4j/conf:/conf   
    environment:
      - NEO4J_AUTH=neo4j/12345678
      - NEO4JLABS_PLUGINS=["apoc"]
      - NEO4J_dbms_security_procedures_allowlist=apoc.*,gds.*
      - NEO4J_dbms_security_procedures_unrestricted=apoc.*,gds.*
    restart: always

  qdrant:
    image: qdrant/qdrant:v1.15.0
    container_name: qdrant
    ports:
      - "6333:6333"
    volumes:
      - ${Browser}/qdrant/data:/qdrant/storage
    restart: always

  # graph_api:
  #   build: 
  #     context: ./apps/graph_api
  #     dockerfile: Dockerfile
  #   container_name: graph_api
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - NEO4J_URI=bolt://neo4j:7687
  #     - QDRANT_URL=http://qdrant:6333
  #     - OLLAMA_URL=http://ollama:11434
  #   depends_on:
  #     - ollama
  #     - neo4j
  #     - qdrant
  #   restart: always

  # frontend:
  #   build: 
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   container_name: frontend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - graph_api
  #   restart: always

  # frontend:
  #   build: 
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   container_name: frontend
  #   ports:
  #     - "3000:80" # <-- Đã sửa: Nginx chạy ở cổng 80 trong container
  #   depends_on:
  #     - graph_api
  #   restart: always

volumes:
  ollama_data:
