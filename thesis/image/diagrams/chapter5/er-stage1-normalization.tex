\documentclass[tikz, border=8pt]{standalone}
\usepackage{tikz}
\usepackage[utf8]{vietnam}
\usetikzlibrary{arrows.meta, positioning, calc}

% Professional, low-saturation palette, aligned with Figure 4.1
\definecolor{clrData}{RGB}{229,241,255}
\definecolor{clrContext}{RGB}{236,253,245}
\definecolor{clrPrompt}{RGB}{245,246,248}
\definecolor{clrExtract}{RGB}{244,240,255}
\definecolor{clrOutput}{RGB}{255,247,237}
\definecolor{clrControl}{RGB}{245,246,248}
\definecolor{clrFeedback}{RGB}{255,248,225}

\definecolor{bdrData}{RGB}{59,130,246}
\definecolor{bdrContext}{RGB}{16,185,129}
\definecolor{bdrPrompt}{RGB}{100,116,139}
\definecolor{bdrExtract}{RGB}{124,58,237}
\definecolor{bdrOutput}{RGB}{234,88,12}
\definecolor{bdrControl}{RGB}{100,116,139}
\definecolor{bdrFeedback}{RGB}{217,119,6}

\tikzset{
  stage/.style={
    rectangle, rounded corners=4pt,
    minimum width=3.35cm, minimum height=5.34cm,
    text width=2.78cm,
    font=\sffamily, align=center,
    inner sep=6pt, text depth=0.2ex,
    line width=0.55pt
  },
  data/.style={stage, fill=clrData, draw=bdrData},
  context/.style={stage, fill=clrContext, draw=bdrContext},
  prompt/.style={stage, fill=clrPrompt, draw=bdrPrompt},
  extract/.style={stage, fill=clrExtract, draw=bdrExtract},
  output/.style={stage, fill=clrOutput, draw=bdrOutput},
  control/.style={
    rectangle, rounded corners=4pt,
    minimum width=10.70cm, minimum height=1.05cm,
    text width=10.10cm,
    font=\scriptsize\sffamily, align=center,
    inner sep=6pt, fill=clrControl, draw=bdrControl,
    dashed, line width=0.5pt
  },
  feedback/.style={
    rectangle, rounded corners=4pt,
    minimum width=7.40cm, minimum height=0.96cm,
    text width=6.90cm,
    font=\scriptsize\sffamily, align=center,
    inner sep=5pt, fill=clrFeedback, draw=bdrFeedback,
    line width=0.5pt
  },
  arr/.style={-{Stealth[length=4pt, width=3pt]}, line width=0.62pt, gray!75},
  support/.style={-{Stealth[length=4pt, width=3pt]}, line width=0.52pt, dashed, gray!68},
  loop/.style={-{Stealth[length=4pt, width=3pt]}, line width=0.55pt, dashed, bdrFeedback!85}
}

\newcommand{\stagetitle}[1]{{\bfseries\small #1}\\[6pt]}
\newcommand{\stageitem}[1]{{\scriptsize #1}\\[3pt]}
\begin{document}
\begin{tikzpicture}[node distance=0.62cm and 0.56cm]
\node[data] (IN) {
    \stagetitle{Thực thể thô}
    \stageitem{Tên chính}
    \stageitem{Aliases}
    \stageitem{Labels}
    \stageitem{Description}
    {\scriptsize \texttt{chunk\_id}, nguồn}
  };

  \node[context, right=of IN] (MERGE) {
    \stagetitle{Đọc đồ thị thô}
    \stageitem{Tải các thực thể}
    \stageitem{Giữ định danh gốc}
    \stageitem{Giữ nhãn loại}
    \stageitem{Giữ nguồn dữ liệu}
    {\scriptsize Bản ghi đầu vào}
  };

  \node[prompt, right=of MERGE] (NORM) {
    \stagetitle{Chuẩn hóa văn bản}
    \stageitem{Tên chính}
    \stageitem{Tên gọi thay thế}
    \stageitem{Mô tả}
    \stageitem{Khoảng trắng, ký tự}
    {\scriptsize Chưa quyết định hợp nhất}
  };

  \node[extract, right=of NORM] (EMB) {
    \stagetitle{Tạo biểu diễn}
    \stageitem{Dựa trên tên gọi}
    \stageitem{Xử lý tiền tố tên người}
    \stageitem{Biểu diễn ngữ nghĩa}
    \stageitem{Có phương án dự phòng}
    {\scriptsize Phục vụ so sánh}
  };

  \node[output, right=of EMB] (OUT) {
    \stagetitle{Lưu bản ghi nhúng}
    \stageitem{Vector thực thể}
    \stageitem{Thông tin đi kèm}
    \stageitem{Thuộc tính đã chuẩn hóa}
    \stageitem{Bằng chứng nguồn}
    {\scriptsize Sang giai đoạn 2}
  };

  \node[control] (RULE) at ($(MERGE.north)!0.55!(EMB.north)+(0,1.05cm)$) {
    {\bfseries Quy tắc chuẩn hóa thực thể}\\[-1pt]
    {Chỉ chuẩn hóa dữ liệu và tạo biểu diễn so sánh, không tạo tri thức mới và không hợp nhất ở giai đoạn này}
  };

  \node[feedback] (PROV) at ($(NORM.south)!0.55!(EMB.south)+(0,-1.02cm)$) {
    {\bfseries Giữ thông tin truy vết}\\[-1pt]
    {Tên gọi, mô tả, nguồn và thông tin đoạn dữ liệu được bảo toàn cho các bước sau}
  };

  \draw[arr] (IN) -- (MERGE);
  \draw[arr] (MERGE) -- (NORM);
  \draw[arr] (NORM) -- (EMB);
  \draw[arr] (EMB) -- (OUT);

  \draw[support] (RULE.south west) -- (MERGE.north);
  \draw[support] (RULE.south) -- (NORM.north);
  \draw[support] (RULE.south east) -- (EMB.north);
  \draw[loop] (NORM.south) -- (PROV.north west);
  \draw[loop] (EMB.south) -- (PROV.north east);
  \draw[loop] (PROV.east) -| (OUT.south);

\end{tikzpicture}
\end{document}
