\documentclass[tikz, border=8pt]{standalone}
\usepackage{tikz}
\usepackage[utf8]{vietnam}
\usetikzlibrary{arrows.meta, positioning, calc}

% Professional, low-saturation palette, aligned with Figure 4.1
\definecolor{clrData}{RGB}{229,241,255}
\definecolor{clrContext}{RGB}{236,253,245}
\definecolor{clrPrompt}{RGB}{245,246,248}
\definecolor{clrExtract}{RGB}{244,240,255}
\definecolor{clrOutput}{RGB}{255,247,237}
\definecolor{clrControl}{RGB}{245,246,248}
\definecolor{clrFeedback}{RGB}{255,248,225}

\definecolor{bdrData}{RGB}{59,130,246}
\definecolor{bdrContext}{RGB}{16,185,129}
\definecolor{bdrPrompt}{RGB}{100,116,139}
\definecolor{bdrExtract}{RGB}{124,58,237}
\definecolor{bdrOutput}{RGB}{234,88,12}
\definecolor{bdrControl}{RGB}{100,116,139}
\definecolor{bdrFeedback}{RGB}{217,119,6}

\tikzset{
  stage/.style={
    rectangle, rounded corners=4pt,
    minimum width=3.35cm, minimum height=5.34cm,
    text width=2.78cm,
    font=\sffamily, align=center,
    inner sep=6pt, text depth=0.2ex,
    line width=0.55pt
  },
  data/.style={stage, fill=clrData, draw=bdrData},
  context/.style={stage, fill=clrContext, draw=bdrContext},
  prompt/.style={stage, fill=clrPrompt, draw=bdrPrompt},
  extract/.style={stage, fill=clrExtract, draw=bdrExtract},
  output/.style={stage, fill=clrOutput, draw=bdrOutput},
  control/.style={
    rectangle, rounded corners=4pt,
    minimum width=10.70cm, minimum height=1.05cm,
    text width=10.10cm,
    font=\scriptsize\sffamily, align=center,
    inner sep=6pt, fill=clrControl, draw=bdrControl,
    dashed, line width=0.5pt
  },
  feedback/.style={
    rectangle, rounded corners=4pt,
    minimum width=7.40cm, minimum height=0.96cm,
    text width=6.90cm,
    font=\scriptsize\sffamily, align=center,
    inner sep=5pt, fill=clrFeedback, draw=bdrFeedback,
    line width=0.5pt
  },
  arr/.style={-{Stealth[length=4pt, width=3pt]}, line width=0.62pt, gray!75},
  support/.style={-{Stealth[length=4pt, width=3pt]}, line width=0.52pt, dashed, gray!68},
  loop/.style={-{Stealth[length=4pt, width=3pt]}, line width=0.55pt, dashed, bdrFeedback!85}
}

\newcommand{\stagetitle}[1]{{\bfseries\small #1}\\[6pt]}
\newcommand{\stageitem}[1]{{\scriptsize #1}\\[3pt]}
\begin{document}
\begin{tikzpicture}[node distance=0.62cm and 0.56cm]
\node[data] (IN) {
    \stagetitle{Thực thể đã nhúng}
    \stageitem{Tên đã chuẩn hóa}
    \stageitem{Nhãn loại thực thể}
    \stageitem{Biểu diễn ngữ nghĩa}
    \stageitem{Thông tin nguồn}
    {\scriptsize Đầu ra giai đoạn 1}
  };

  \node[context, right=of IN] (LABEL) {
    \stagetitle{Tạo khối so sánh}
    \stageitem{Dựa trên nhãn loại}
    \stageitem{Gom loại tương thích}
    \stageitem{Tách loại dễ nhầm}
    \stageitem{Thu hẹp phạm vi}
    {\scriptsize Khối ứng viên}
  };

  \node[prompt, right=of LABEL] (ANN) {
    \stagetitle{So sánh trong khối}
    \stageitem{Lấy các vector liên quan}
    \stageitem{Đo độ gần nhau}
    \stageitem{Loại cặp xa nhau}
    \stageitem{Giữ cặp tiềm năng}
    {\scriptsize Tập cần gom cụm}
  };

  \node[extract, right=of ANN] (CLUSTER) {
    \stagetitle{Gom cụm ứng viên}
    \stageitem{Nhóm thực thể gần nhau}
    \stageitem{Có cơ chế dự phòng}
    \stageitem{Giữ riêng điểm nhiễu}
    \stageitem{Ưu tiên không bỏ sót}
    {\scriptsize Cụm ứng viên}
  };

  \node[output, right=of CLUSTER] (OUT) {
    \stagetitle{Đầu ra giai đoạn 2}
    \stageitem{Danh sách cụm}
    \stageitem{Thực thể trong cụm}
    \stageitem{Thông tin đánh giá}
    \stageitem{Nguồn khối so sánh}
    {\scriptsize Sang giai đoạn 3}
  };

  \node[control] (RULE) at ($(LABEL.north)!0.55!(CLUSTER.north)+(0,1.05cm)$) {
    {\bfseries Gom khối trước khi tạo cụm}\\[-1pt]
    {Chỉ so sánh trong phạm vi loại thực thể phù hợp để giảm nhiễu và tránh ghép sai kiểu}
  };

  \node[feedback] (NOISE) at ($(ANN.south)!0.55!(CLUSTER.south)+(0,-1.02cm)$) {
    {\bfseries Kiểm soát nhiễu}\\[-1pt]
    {Thực thể không đủ gần với nhóm nào được giữ riêng, không bị ép thành ứng viên hợp nhất}
  };

  \draw[arr] (IN) -- (LABEL);
  \draw[arr] (LABEL) -- (ANN);
  \draw[arr] (ANN) -- (CLUSTER);
  \draw[arr] (CLUSTER) -- (OUT);

  \draw[support] (RULE.south west) -- (LABEL.north);
  \draw[support] (RULE.south) -- (ANN.north);
  \draw[support] (RULE.south east) -- (CLUSTER.north);
  \draw[loop] (CLUSTER.south) -- (NOISE.north east);
  \draw[loop] (NOISE.north west) -- (ANN.south);

\end{tikzpicture}
\end{document}
